version: "2"

linters:
  default: none
  enable:
    - durationcheck
    - govet
    - nilerr
    - nolintlint
  settings:
    nolintlint:
      require-explanation: true
      require-specific: true
  exclusions:
    generated: strict

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
