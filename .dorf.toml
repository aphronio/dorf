[commands]
prepare = "UV_CACHE_DIR=.dorf/uv-cache uv sync --frozen --all-groups && go mod download"
check = "scripts/verify-issue-37-legacy-deletion.sh && go test ./... && go vet ./... && UV_CACHE_DIR=.dorf/uv-cache uv run pytest && UV_CACHE_DIR=.dorf/uv-cache uv run ruff check ."
smoke = "mkdir -p .dorf/bin && go build -o .dorf/bin/dorf ./cmd/dorf && go version -m .dorf/bin/dorf"

[agent.codex]
model = "gpt-5.6-sol"
reasoning_effort = "low"

[incus]
template = "dorf-codex"
