[commands]
prepare = "scripts/dev/prepare.sh"
check = "scripts/dev/check.sh"
smoke = "mkdir -p .dorf/bin && go build -o .dorf/bin/dorf ./cmd/dorf && .dorf/bin/dorf version && go version -m .dorf/bin/dorf"

[agent.codex]
model = "gpt-5.6-sol"
reasoning_effort = "low"

[incus]
template = "dorf-codex"
