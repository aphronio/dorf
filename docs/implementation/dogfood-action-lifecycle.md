# Sandbox Lifecycle Actions

Dorf records external Sandbox lifecycle operations as durable Actions, preserving their progress and outcomes across interruption and recovery. Sandbox records remain the authoritative home for each Sandbox's stable identity and ownership, while Actions capture the lifecycle work performed against it.
